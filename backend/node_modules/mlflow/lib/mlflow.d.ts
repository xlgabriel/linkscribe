export interface ConstructorProps {
    endpoint: string;
    headers?: HeadersInit;
    version?: string;
}
export declare class MLflowBase {
    endpoint: string;
    headers?: HeadersInit;
    version?: string;
    protected path: string;
    constructor({ endpoint, headers, version }: ConstructorProps);
    protected requestUrl(path: string): string;
    protected req(method: string, path: string, param: any, headers?: HeadersInit): Promise<any>;
}
export default class MLflow extends MLflowBase {
    constructor(args: ConstructorProps);
    readonly Experiments: Experiments;
    readonly Runs: Runs;
    readonly Metrics: Metrics;
    readonly Artifacts: Artifacts;
}
import Experiments from './experiments';
import Runs from './runs';
import Metrics from './metrics';
import Artifacts from './artifacts';
